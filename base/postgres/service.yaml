apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: "postgres"
      protocol: "TCP"
      port: 5432
      targetPort: 5432
  selector:
    app: postgres
