# clusters/

Per-cluster manifests and bootstrap materials, laid out as `clusters/<name>/`.

- `pi/`: single-node Raspberry Pi 5 cluster — Argo CD Applications, platform
  manifests, Cilium values, and bootstrap steps
